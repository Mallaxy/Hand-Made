import { Container } from "@mui/material";
import { TodoList } from "@subproject/src/components/TodoList";

const App = () => {
  return (
    <Container>
      <TodoList />
    </Container>
  );
};

export default App;
